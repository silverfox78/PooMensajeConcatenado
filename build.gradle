group 'ciisa.control'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

before_install:
 - chmod +x gradlew

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
